Using Xtricorder and Xtriage from HKLviewer for inspecting TNCS, anisotropy and twinning.

here we will demonstrate how to use the hql view to inspect problematic data such as datafr3 rbx.nz which we have already loaded data sets available are listed on the details tab and if we have Phoenix installed then on the quick view tab here we have these two buttons X triage and xtricoder so now let's try and run extra code to analyze the data sets in more detail relax and once it's done a scale view will will load a new file with a new data sets produced by x three quarter where the most interesting data sets are the tncs and the anisotropy correction terms if we go back to the quick view tab here we'll have these new buttons here and we can try and rotate around the cncs vector to display the data how it would look with the TNTs correction tones um so we can see this very strong modulation here in the data so every three layer and the data is quite strong and the two middle layers it's a relatively weak so if we were to compute e values based on this data we would get something looking like this if we just press the E values button here in the quick view Tab and we can see for the E values even there there's quite strong modulation here so this data here would be no good unless we apply the tncs correction terms now correcting the data is all done internally by phaser but let's try and manually do the TNTs correction here let's first display the intensities and we go to the details Tab and find the intensities and then we right click here and make a new data set and we type new array um dot underscore data equals array one Dot data divided by every two Dot and uh so array 1 corresponds to the intensities here and array 2 will associate with the tncs correction terms find here and we give it a suitable name the new array I will call it I over tdps and unless I've made a mistake this should compute so here we have data that our intensities with the TNTs correction terms applied but we also want to normalize it so we can better compare it to the other normalized data so we make a new data set again here and type array one dot normalize dot data and we call this e for E values e underscore I over T APS and they should compute so this should be e values where we have applied the TNTs Corrections so if we have a look at the rotate around cncs effect and try and rotate the BCE values around do that by going here and clicking on the uncorrected E values and rotate them as well you can see that there's strong ripples in the data corresponding to the TNTs correction now if we go to the corrected e values and rotate those around we can see that there are no rivers in the data so that concludes that we actually apply the tncs correction to the data and this data would be suitable for using in the molecular replacement search now let's show how to inspect the twin door so let's load them Phoenix tutorial data set called pouring which has got some twinning let me go back to the quick view tab here show the amplitudes data and then we run Phoenix X triage an extra yards tell us that there are these possible twin operators here and the one with the lowest r factor which is the last one this is the one that we're interested in and we can go here on the details tab and find the vector tap and see that this is actually that twin operator here so we go back to quick view again and let's slice amplitudes perpendicular to that twin operator and what we should pay attention to is that when we step through layers of reflections then according to the color coding we should see Central Symmetry and that appears to be the case so that indicates that this data set is actually twinned what we could also do is to bring up the table of reflections for the data set and then right clicking one data point here we find that the value here is 108. and the twin related value we can find from its tip being 901 so that indicates that these are within the same order of magnitudes these two values here and um yeah confirms that it's twin related

